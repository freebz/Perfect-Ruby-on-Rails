<%# 리스트 9-12 ajax/search.html.erb %>

<%= form_tag({ action: 'result' }, remote: true) do %>
  <%= select_tag(:publish, options_from_collection_for_select(
    @books, :publish, :publish, '제이펍')) %>
  <%= submit_tag '검색' %>
<% end %>
<!-- 결과를 출력하기 위한 목록 -->
<ul id="result"></ul>
