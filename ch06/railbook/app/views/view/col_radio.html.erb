<%# 리스트 4-21 view/col_radio.html.erb %>

<%= form_for(@book) do |f| %>
  <%= f.collection_check_boxes(:publish, @books, :publish, :publish) %><br />
  <%= f.collection_radio_buttons(:publish, @books, :publish, :publish) %>
<% end %>
