# 리스트 10-55 httpd.conf

# Example:
# LoadModule foo_module modules/mod_foo.so

LoadModule passenger_module /usr/local/lib/ruby/gems/2.0.0/gems/passenger-4.2.06/buildout/apache2/mod_passenger.so
PassengerRoot /usr/local/lib/ruby/gems/2.0.0/gems/passenger-4.2.06
PassengerDefaultRuby /usr/local/bin/ruby
...생략...
# DocumentRoot: The directory out of which you will serve your
# documents. By default, all requests are taken from this directory, but
# symbolic links and aliases may be used to point to other locations.

DocumentRoot "/var/www/html/railbook/public"
